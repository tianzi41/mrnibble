# 知伴（ZhiBan）运行时依赖（锁定版本，实测 win_amd64 / Python 3.13）
# 安装：pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# ── 后端框架 ───────────────────────────────────────────
fastapi==0.141.1
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-multipart>=0.0.9

# ── 配置与网络 ─────────────────────────────────────────
python-dotenv>=1.0.1
httpx>=0.28.1

# ── 文档解析 ───────────────────────────────────────────
pypdf>=4.2.0
python-docx>=1.1.2
python-pptx>=0.6.23
beautifulsoup4>=4.12.3
lxml>=5.2.0

# ── 存储与数值 ─────────────────────────────────────────
numpy>=1.26.0

# ── 中文分词 ───────────────────────────────────────────
jieba>=0.42.1

# ── 密钥加密 ───────────────────────────────────────────
cryptography>=42.0.0

# ── 导出 ───────────────────────────────────────────────
genanki>=0.13.1
XlsxWriter>=3.2.0

# ── 图像（导出图片/OCR 兜底读取） ──────────────────────
Pillow>=10.3.0

# ── 本地语音识别（ASR，纯本地）────────────────────────
sherpa-onnx>=1.13.8

# ── 其它 ───────────────────────────────────────────────
pyyaml>=6.0.1
